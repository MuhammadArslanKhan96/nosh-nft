"use client";
export const verifyUser = (path: string) => {
  // useEffect(() => {
  //   const loginToken = localStorage.getItem("loginToken");
  //   const userId = localStorage.getItem("userId");
  //   if (loginToken && userId) {
  //     redirect(path);
  //   } else {
  //     toast.error("You need to login first");
  //   }
  // }, []);
};
